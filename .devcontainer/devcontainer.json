{
    "name": "Final Project ROS",
    "image": "ros:rolling-perception-noble",
    "features": {
    },
    "extensions": [
    ],
    "settings": {
    },
    "postCreateCommand": "echo 'source /ros_entrypoint.sh' >> ~/.bashrc && apt-get update && apt-get install -y vim"
  }